{
  "name": "alfred",
  "version": "0.1.0",
  "private": true,
  "description": "WhatsApp-first personal agent core runtime",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "npm run test:unit && npm run test:integration && npm run test:smoke && npm run test:security",
    "test:unit": "vitest run tests/unit --reporter=dot",
    "test:integration": "vitest run tests/integration --reporter=dot",
    "test:smoke": "vitest run tests/smoke --reporter=dot",
    "test:security": "vitest run tests/security --reporter=dot",
    "dev:gateway": "tsx apps/gateway-orchestrator/src/server.ts",
    "dev:worker": "tsx apps/worker/src/server.ts"
  },
  "dependencies": {
    "express": "^5.2.1",
    "supertest": "^7.2.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.0",
    "@types/supertest": "^6.0.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
